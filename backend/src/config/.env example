# Configuración del servidor
PORT=5000
HOST=localhost

# Configuración de la base de datos PostgreSQL
DB_HOST=localhost
DB_NAME=pern_template
DB_USER=postgres
DB_PASSWORD=admin123

# JWT Secrets (cambia estos por valores únicos y seguros)
ACCESS_JWT_SECRET=your-super-secret-access-token-key-2024
REFRESH_JWT_SECRET=your-super-secret-refresh-token-key-2024
